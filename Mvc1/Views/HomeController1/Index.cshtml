
@{
    ViewData["Title"] = "Index";
}

<h1>Demo Index</h1>

<table>
    @for (int y = 0; y <  10; y++)
    {
        <tr>
            @for (int x = 1; x <= 10; x++)
            {
                <td>@(y*x)</td>
            }
        </tr>
    }
</table>

